---
description: Pipeline for processing Kolide audit log events from the REST API (CEL) and webhooks.
processors:
  - set:
      field: ecs.version
      tag: set_ecs_version
      value: '9.3.0'

  # --- CEL input only ---
  - remove:
      field:
        - organization
        - division
        - team
      ignore_missing: true
      if: ctx.organization instanceof String && ctx.division instanceof String && ctx.team instanceof String
      tag: remove_agentless_tags
      description: >-
        Removes the fields added by Agentless as metadata,
        as they can collide with ECS fields.
  - terminate:
      tag: data_collection_error
      if: ctx.error?.message != null && ctx.message == null && ctx.event?.original == null
      description: error message set and no data to process.
  # --- end CEL-only ---

  # Webhook path: the body is delivered at the document root, where the `event`
  # key is a string that would collide with the ECS `event` object. Move the
  # envelope into the working `json` object so both inputs converge on the same shape.
  - rename:
      field: event
      tag: rename_webhook_event_envelope
      target_field: json.event
      ignore_missing: true
      if: ctx.event instanceof String
      description: Move the webhook envelope `event` (a string) out of the way of the ECS `event` object.
  - script:
      lang: painless
      tag: move_webhook_envelope_to_json
      if: ctx.json?.event != null && ctx.data != null
      description: Move the remaining webhook envelope fields into the working json object. event.original is set by the input when preserve_original_event is enabled.
      source: |-
        if (ctx.containsKey('id')) { ctx.json.id = ctx.remove('id'); }
        if (ctx.containsKey('timestamp')) { ctx.json.timestamp = ctx.remove('timestamp'); }
        if (ctx.containsKey('data')) { ctx.json.data = ctx.remove('data'); }

  # --- CEL / JSE00001 path: message holds the raw JSON of one audit_log object ---
  - rename:
      field: message
      tag: rename_message_to_event_original
      target_field: event.original
      ignore_missing: true
      description: Renames the original `message` field to `event.original` to store a copy of the original message. The `event.original` field is not touched if the document already has one; it may happen when Logstash sends the document.
      if: ctx.event?.original == null
  - remove:
      field: message
      tag: remove_message
      ignore_missing: true
      description: The `message` field is no longer required if the document has an `event.original` field.
      if: ctx.event?.original != null
  - json:
      field: event.original
      target_field: json
      tag: parse_json_cel
      if: ctx.json == null && ctx.event?.original != null
      description: Decode the CEL message (a JSON string of one audit_log object) into the working json object.

  # --- Common normalization ---
  # @timestamp from the envelope/object timestamp.
  - date:
      field: json.timestamp
      target_field: '@timestamp'
      formats:
        - ISO8601
      tag: date_timestamp
      if: ctx.json?.timestamp != null

  # event.id: webhook envelope ULID (json.id) / API audit_log id (json.id).
  - rename:
      field: json.id
      target_field: event.id
      tag: rename_event_id
      ignore_missing: true

  # event.action: webhook event value (audit_log.recorded), or `audit_log` for the API path.
  - rename:
      field: json.event
      target_field: event.action
      tag: rename_event_action_webhook
      ignore_missing: true
  - set:
      field: event.action
      tag: set_event_action_api
      value: audit_log
      if: ctx.event?.action == null

  # --- ECS categorization ---
  - set:
      field: event.kind
      tag: set_event_kind
      value: event
  - append:
      field: event.category
      tag: append_event_category
      value:
        - iam
        - configuration
  - append:
      field: event.type
      tag: append_event_type
      value:
        - change

  # --- Actor (user) ---
  # Webhook delivers actor_* under json.data; the API delivers actor_info plus a
  # top-level actor_name/description on the audit_log object.
  - rename:
      field: json.data.actor_name
      target_field: user.name
      tag: rename_user_name_webhook
      ignore_missing: true
  - rename:
      field: json.actor_info.actor_name
      target_field: user.name
      tag: rename_user_name_api
      ignore_missing: true
      if: ctx.user?.name == null
  - rename:
      field: json.actor_name
      target_field: user.name
      tag: rename_user_name_api_toplevel
      ignore_missing: true
      if: ctx.user?.name == null
  - remove:
      field: json.actor_name
      tag: remove_actor_name_toplevel
      ignore_missing: true

  - rename:
      field: json.data.actor_email
      target_field: user.email
      tag: rename_user_email_webhook
      ignore_missing: true
  - rename:
      field: json.actor_info.actor_email
      target_field: user.email
      tag: rename_user_email_api
      ignore_missing: true
      if: ctx.user?.email == null

  - rename:
      field: json.data.actor_type
      target_field: kolide.audit.actor_type
      tag: rename_actor_type_webhook
      ignore_missing: true
  - rename:
      field: json.actor_info.actor_type
      target_field: kolide.audit.actor_type
      tag: rename_actor_type_api
      ignore_missing: true
      if: ctx.kolide?.audit?.actor_type == null

  # --- Source IP (webhook only) ---
  - rename:
      field: json.data.ip_address
      target_field: source.ip
      tag: rename_source_ip_webhook
      ignore_missing: true
  - geoip:
      field: source.ip
      target_field: source.geo
      ignore_missing: true
      tag: enrich_source_geo
      if: ctx.source?.ip != null && ctx.source?.geo == null
  - geoip:
      database_file: GeoLite2-ASN.mmdb
      field: source.ip
      target_field: source.as
      properties:
        - asn
        - organization_name
      ignore_missing: true
      tag: enrich_source_asn
      if: ctx.source?.ip != null && ctx.source?.as == null
  - rename:
      field: source.as.asn
      target_field: source.as.number
      ignore_missing: true
      tag: rename_source_asn
  - rename:
      field: source.as.organization_name
      target_field: source.as.organization.name
      ignore_missing: true
      tag: rename_source_as_org

  # --- message / description ---
  - rename:
      field: json.data.description
      target_field: message
      tag: rename_message_webhook
      ignore_missing: true
  - rename:
      field: json.description
      target_field: message
      tag: rename_message_api
      ignore_missing: true
      if: ctx.message == null

  # --- related.* enrichment ---
  - append:
      field: related.user
      tag: append_related_user_name
      value: '{{{user.name}}}'
      allow_duplicates: false
      if: ctx.user?.name != null
  - append:
      field: related.user
      tag: append_related_user_email
      value: '{{{user.email}}}'
      allow_duplicates: false
      if: ctx.user?.email != null
  - append:
      field: related.ip
      tag: append_related_ip_source
      value: '{{{source.ip}}}'
      allow_duplicates: false
      if: ctx.source?.ip != null

  # --- Extended field extraction from message/description ---
  - pipeline:
      name: '{{ IngestPipeline "extended-mappings" }}'
      tag: extended_mappings
      ignore_missing_pipeline: true

  # --- Cleanup ---
  - remove:
      field:
        - json
        - data
      tag: remove_temp_fields
      ignore_missing: true
  - append:
      field: tags
      tag: append_preserve_original_event
      value: preserve_original_event
      allow_duplicates: false
      if: ctx.error?.message != null

on_failure:
  - append:
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{{#_ingest.on_failure_processor_tag}}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{{/_ingest.on_failure_processor_tag}}}failed with message '{{{ _ingest.on_failure_message }}}'
  - set:
      field: event.kind
      tag: set_pipeline_error_to_event_kind
      value: pipeline_error
  - append:
      field: tags
      value: preserve_original_event
      allow_duplicates: false
