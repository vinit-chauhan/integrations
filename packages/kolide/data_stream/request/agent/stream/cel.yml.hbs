interval: {{interval}}
resource.tracer:
  enabled: {{enable_request_tracer}}
  filename: "../../logs/cel/http-request-trace-*.ndjson"
  maxbackups: 5
{{#if proxy_url}}
resource.proxy_url: {{proxy_url}}
{{/if}}
{{#if ssl}}
resource.ssl: {{ssl}}
{{/if}}
{{#if http_client_timeout}}
resource.timeout: {{http_client_timeout}}
{{/if}}
resource.url: {{url}}
resource.headers:
  Accept:
    - application/json
  X-Kolide-Api-Version: "2026-04-07"
state:
  api_key: {{api_key}}
  batch_size: {{batch_size}}
redact:
  fields:
    - api_key
program: |-
  // Paginated request snapshots. Walk exemption requests first, then registration requests.
  state.?cursor.kind.orValue("exemption").as(kind,
    state.?cursor.next.orValue("").as(next_token,
      state.with(
        request(
          "GET",
          state.url.trim_right("/") + (
            (kind == "registration") ?
              "/registration_requests?"
            :
              "/exemption_requests?"
          ) + (
            (next_token != "") ?
              {
                "per_page": [string(int(state.batch_size))],
                "cursor": [next_token],
              }
            :
              {
                "per_page": [string(int(state.batch_size))],
              }
          ).format_query()
        ).with(
          {
            "Header": {
              "Authorization": ["Bearer " + state.api_key],
            },
          }
        ).do_request().as(resp,
          (resp.StatusCode == 200) ?
            resp.Body.decode_json().as(body,
              body.?pagination.next_cursor.orValue("").as(next_cursor,
                body.?data.orValue([]).map(e, {"message": e.with({"_kolide_request_type": kind}).encode_json()}).as(events,
                  (next_cursor != "" || kind == "exemption").as(want_more,
                    {
                      "events": (want_more && size(events) == 0) ? [{"message": "{}", "retry": "true"}] : events,
                      "cursor": {
                        "kind": (next_cursor != "") ? kind : ((kind == "exemption") ? "registration" : "exemption"),
                        "next": next_cursor,
                      },
                      "want_more": want_more,
                    }
                  )
                )
              )
            )
          :
            {
              "events": {
                "error": {
                  "code": string(resp.StatusCode),
                  "id": string(resp.Status),
                  "message": "GET " + ((kind == "registration") ? "/registration_requests" : "/exemption_requests") + ": " + (
                    (size(resp.Body) != 0) ?
                      string(resp.Body)
                    :
                      string(resp.Status) + " (" + string(resp.StatusCode) + ")"
                  ),
                },
              },
              "want_more": false,
            }
        )
      )
    )
  )
tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
publisher_pipeline.disable_host: true
{{/contains}}
processors:
  - drop_event.when.equals.retry: "true"
{{#if processors}}
{{processors}}
{{/if}}
